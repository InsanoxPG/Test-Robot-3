package frc.robot;
/// RobotState is new RobotState(listOfSubsystems)
/// interp. the current state of the robot in terms of all its subsystems

public class RobotState {
    /* TEMPLATE FUNCTION
    private void fnForRobotState(RobotState state) {
        fnForListOfSubsystems(state);
    }
    */
}
